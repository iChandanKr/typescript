// This is type inference in ts it automatically detect the immediate type of assigned value if 
let myName = "chandan";

let charl: string;
function findName() {
    return 'hritik';
}
charl = findName();

console.log(charl);
